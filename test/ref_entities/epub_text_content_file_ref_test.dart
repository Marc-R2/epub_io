import 'package:archive/archive.dart';
import 'package:epub_io/epub_io.dart';
import 'package:epub_io/src/ref_entities/epub_text_content_file_ref.dart';
import 'package:test/test.dart';

void main() {
  /* final arch = Archive();
  final epubRef = EpubBookRef(epubArchive: arch);

  final reference = EpubTextContentFileRef(
    epubBookRef: epubRef,
    contentMimeType: 'application/test',
    contentType: EpubContentType.other,
    fileName: 'orthrosFile',
  );

  late EpubTextContentFileRef testFile;

  setUp(() {
    final arch2 = Archive();
    final epubRef2 = EpubBookRef(epubArchive: arch2);

    testFile = reference.copyWith(epubBookRef: epubRef2);
  }); */

  group('EpubTextContentFile', () {
    // TODO(Marc-R2): Removed redundant tests
  });
}
