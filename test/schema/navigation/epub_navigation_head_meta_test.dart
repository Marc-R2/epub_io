import 'package:test/test.dart';

void main() {
  // final generator = RandomDataGenerator(Random(7898), 10);
  // final reference = generator.randomNavigationHeadMeta();

  setUp(() {});

  group('EpubNavigationHeadMeta', () {
    // TODO(Marc-R2): Removed redundant tests
  });
}
